#Part 14 // Listen erstellen, bearbeiten und Initialisieren

#eine Liste ist genau wie der Intiger eine Variable aber wie der Name sagt eine Auflistung von Variablen
#also eine Liste an objekten z.B. int, str, oder klassen

variable_test = 10

list_numbers = [3, 4, 34, 5, 345, 6, 3456] # eine Liste wird durch den bezeichner und [] deklariert und in die [] werden
#dann die werte gespeichert, es muss nicht direkt beim erstellen passieren, es können auch später variablen hinzugefügt
#oder entfernt werden

print(type(list_numbers)) # gibt den Datentyp aus // <class 'list'>

list_names = ['Fritz', 'Inessa', 'Susi', 'Simon']

print(list_numbers)
print(list_names)
#es kann jeder Datentyp in einer Liste gespeichert werden, auch mehere verschiedene wie z.B.
list_random = ['ABC', 33, 'DEF', 1.23, 44]
print(list_random)
